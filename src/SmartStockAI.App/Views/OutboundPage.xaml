<Page x:Class="SmartStockAI.App.Views.OutboundPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="Transparent"
      Title="Расход">
    <ScrollViewer Style="{StaticResource PageScrollViewerStyle}">
    <Grid Margin="0,0,0,8">
        <Border Style="{StaticResource PageContainerStyle}">
            <StackPanel>
                <TextBlock Text="DOCUMENT FLOW"
                           Style="{StaticResource SectionEyebrowTextStyle}" />
                <TextBlock Margin="0,10,0,0"
                           Text="Расход"
                           Style="{StaticResource PageTitleTextStyle}" />
                <TextBlock Margin="0,8,0,0"
                           Text="Здесь появится спокойный экран расхода с заметными предупреждениями о нехватке и читаемым составом документа."
                           Style="{StaticResource PageDescriptionTextStyle}" />
                <Button Margin="0,18,0,0"
                        HorizontalAlignment="Left"
                        Content="Новый расход"
                        Style="{StaticResource PrimaryButtonStyle}" />
                <TextBlock Margin="0,28,0,0"
                           Text="Пустой экран расхода."
                           Style="{StaticResource PlaceholderTextStyle}" />
            </StackPanel>
        </Border>
    </Grid>
    </ScrollViewer>
</Page>
